[package]
name = "eq-state-compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
eq-memory = { path = "../eq-memory" }

[lib]
name = "eq_state_compiler"
path = "src/lib.rs"
